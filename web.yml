zuul:
  retryable: true
  routes:
    races: /races/**
    participants: /paticipants/**

ribbon:
  ConnectTimeout: 60000
  ReadTimeout: 60000
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
        # isolation:
        #   thread:
        #     timeoutInMilliseconds: 60000
